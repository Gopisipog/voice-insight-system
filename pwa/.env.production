# Production backend URL - Change this to your deployed backend
VITE_BACKEND_HOST=voice-insight-backend.onrender.com
# No port needed — Render serves HTTPS/WSS on standard ports (443/80)
VITE_SERVER_PORT=
